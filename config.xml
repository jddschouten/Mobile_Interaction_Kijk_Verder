<?xml version="1.0" encoding="UTF-8"?>
<!-- config.xml reference: https://build.phonegap.com/docs/config-xml -->
<widget xmlns       = "http://www.w3.org/ns/widgets"
		xmlns:gap   = "http://phonegap.com/ns/1.0"
		id          = "nl.avans.aii.42CM06.KijkVerder"
		version     = "1.0.0"
		versionCode = "100"
	> <!--CHANGE id, version (all platforms) and versionCode (Android only)-->

	<name>KijkVerder</name> <!--CHANGE THIS-->

	<description>
		Eerste opzet applicatie <!--CHANGE THIS-->
	</description>

	<author href="http://www.avans.nl">
		Jeroen Schouten | Mark van Osch <!--CHANGE THIS-->
	</author>

	<!-- Platform Build Selection (Optional):
		By default PhoneGap Build builds your application for every platform. If you only want to build for certain platforms you can specify these platforms with the gap:platform tag.
	-->
	<gap:platform name="ios" />
	<gap:platform name="android" />
	<gap:platform name="winphone" />

	<!-- Permissions:
		If you do not want any permissions to be added to your app, add the following tag to your config.xml; you will still have the INTERNET permission on your app, which PhoneGap requires.
	-->
	<preference name="permissions" value="none"/>

	<!-- Customize your app and platform with the preference element. -->
	<preference name="orientation" value="portrait" />
	<preference name="phonegap-version" value="3.6.3" /> <!-- all: current version of PhoneGap -->
	<preference name="orientation" value="default" /> <!-- all: default means both landscape and portrait are enabled -->
	<preference name="target-device" value="universal" /> <!-- all: possible values handset, tablet, or universal -->
	<preference name="fullscreen" value="true" /> <!-- all: hides the status bar at the top of the screen -->
	<preference name="webviewbounce" value="true" /> <!-- ios: control whether the screen 'bounces' when scrolled beyond the top -->
	<preference name="prerendered-icon" value="true" /> <!-- ios: if icon is prerendered, iOS will not apply it's gloss to the app's icon on the user's home screen -->
	<preference name="stay-in-webview" value="false" /> <!-- ios: external links should open in the default browser, 'true' would use the webview the app lives in -->
	<preference name="ios-statusbarstyle" value="black-opaque" /> <!-- ios: black-translucent will appear black because the PhoneGap webview doesn't go beneath the status bar -->
	<preference name="detect-data-types" value="true" /> <!-- ios: controls whether data types (such as phone no. and dates) are automatically turned into links by the system -->
	<preference name="exit-on-suspend" value="false" /> <!-- ios: if set to true, app will terminate when home button is pressed -->
	<preference name="show-splash-screen-spinner" value="true" /> <!-- ios: if set to false, the spinner won't appear on the splash screen during app loading -->
	<preference name="auto-hide-splash-screen" value="true" /> <!-- ios: if set to false, the splash screen must be hidden using a JavaScript API -->
	<preference name="disable-cursor" value="false" /> <!-- blackberry: prevents a mouse-icon/cursor from being displayed on the app -->
	<preference name="android-minSdkVersion" value="7" /> <!-- android: MIN SDK version supported on the target device. MAX version is blank by default. -->
	<preference name="android-installLocation" value="auto" /> <!-- android: app install location. 'auto' will choose. 'internalOnly' is device memory. 'preferExternal' is SDCard. -->

	<!-- PLUGINS: ONLY ADD THOSE NEEDED -->

	<!-- Core plugins -->
	<!-- EXAMPLE
	<gap:plugin name="org.apache.cordova.battery-status" />
	-->

	<!-- Third party plugins -->
		<!-- A list of available plugins are available at https://build.phonegap.com/plugins -->
	<!-- EXAMPLE
	<gap:plugin name="com.phonegap.plugins.barcodescanner" />
	-->

	<!-- Define app icon for each platform. -->
	<icon src="icon128x128.png" />
	<icon src="icon/android36ldpi.png" gap:platform="android" gap:qualifier="ldpi" />
	<icon src="icon/android48mdpi.png" gap:platform="android" gap:qualifier="mdpi" />
	<icon src="icon/android72hdpi.png" gap:platform="android" gap:qualifier="hdpi" />
	<icon src="icon/android96xhdpi.png" gap:platform="android" gap:qualifier="xhdpi" />
	<icon src="icon/blackberry80.png" gap:platform="blackberry" />
	<icon src="icon/blackberry80.png" gap:platform="blackberry" gap:state="hover"/>
	<icon src="icon/ios57x1.png" gap:platform="ios" width="57" height="57" />
	<icon src="icon/ios57x2.png" gap:platform="ios" width="72" height="72" />
	<icon src="icon/ios72x1.png" gap:platform="ios" width="114" height="114" />
	<icon src="icon/ios72x2.png" gap:platform="ios" width="144" height="144" />
	<icon src="icon/webos64.png" gap:platform="webos" />
	<icon src="icon/winphone48.png" gap:platform="winphone" />
	<icon src="icon/winphone173.png" gap:platform="winphone" gap:role="background" />

	<!-- Define app splash screen for each platform. -->
	<gap:splash src="splash/android200x320ldpi.png" gap:platform="android" gap:qualifier="port-ldpi" />
	<gap:splash src="splash/iosandroid320x480mdpi.png" gap:platform="android" gap:qualifier="port-mdpi" />
	<gap:splash src="splash/android480x800hdpi.png" gap:platform="android" gap:qualifier="port-hdpi" />
	<gap:splash src="splash/android720x1280xhdpi.png" gap:platform="android" gap:qualifier="port-xhdpi" />
	<gap:splash src="splash/nexus1080.png" gap:platform="android" gap:qualifier="port-xhdpi" />
	<gap:splash src="splash/blackberry225.png" gap:platform="blackberry" />
	<gap:splash src="splash/iosandroid320x480mdpi.png" gap:platform="ios" width="320" height="480" />
	<gap:splash src="splash/iphone640x960.png" gap:platform="ios" width="640" height="960" />
	<gap:splash src="splash/iphone640x1136.png" gap:platform="ios" width="640" height="1136" />
	<gap:splash src="splash/ipad768x1024.png" gap:platform="ios" width="768" height="1024" />
	<gap:splash src="splash/ipad1024x768.png" gap:platform="ios" width="1024" height="768" />
	<gap:splash src="splash/winphone480x800.jpg" gap:platform="winphone" /> <!-- LET OP: DIT IS OPEENS JPG -->

	<gap:config-file platform="ios" parent="CFBundleShortVersionString">
	<string>100</string>
	</gap:config-file>

	<!--
		Define access to external domains.
		<access /> - a blank access tag denies access to all external resources.
		<access origin="*" /> - a wildcard access tag allows access to all external resource.
		Otherwise, you can specify specific domains:
	-->
	<access origin="*"/>
	<!--
		<access origin="http://phonegap.com" /> - allow any secure requests to http://phonegap.com/
		<access origin="http://phonegap.com" subdomains="true" /> - same as above, but including subdomains, such as http://build.phonegap.com/
		<access origin="http://phonegap.com" browserOnly="true" /> - only allows http://phonegap.com to be opened by the child browser.
	-->

</widget>